# 2026-08-20 정리/리팩터링

- 현재 메인 흐름에서 사용하지 않는 레거시 `/result`, `/food/[id]`, `/taste/conditions` 제거
- 사용하지 않는 과거 추천 컴포넌트 및 피드백/Supabase 코드 제거
- `@supabase/supabase-js` 의존성 제거
- Next.js 기본 샘플 SVG, TypeScript 증분 캐시, Python `__pycache__` 제거
- 누적 `CHANGES_*.md` 파일을 제거하고 현재 구조 문서로 통합
- 지역 포함/제외 판정을 `web/lib/location.ts`로 공통화
- `recommend.ts`에서 현재 서비스가 쓰지 않는 과거 거리/쿼리/결과 페이지용 로직 제거
- LLM 응답에서 UI가 더 이상 사용하지 않는 `reasons` 생성/전송 제거
- 거리 상세에서 항상 빈 배열이던 과거 `nearby` 레이어 제거
- 음식거리 아이콘 경로를 데이터의 `iconPath`로 통합해 코드 하드코딩 중복 제거
- 거리 아이콘 512px PNG를 실제 표시 크기에 맞춰 160px로 최적화(약 4.9MB → 약 0.7MB)
- 깨진 `#Uxxxx` 형태의 한글 원본 파일명을 정상 한글 파일명으로 복구
- 미사용 데이터는 삭제 대신 `data/archive/`로 분류

## 검증

- Python 25개 파일 문법 검사 통과
- TypeScript 핵심 추천/파서/지역 모듈 strict 타입 검사 통과
- TS/TSX 20개 파일 구문 검사 통과
- 로컬 `@/` import 경로 누락 0건
- 음식 296건 / 거리 36건 ID 중복 없음, 거리 아이콘 누락 0건
- 카테고리 1,152개 조합 전수 테스트: 결과 5개 미만 0건, 첫 화면 동일 핵심 식재료 중복 0건
- `다른 추천 보기`: 직전 5개 재노출 0건, 한 화면 동일 식재료 3개 이상 0건
- 지역 부정 테스트: `광주 말고`, `광주 말고 여수`, `여수 말고 순천` 정상 해석 확인

전체 `next build`는 이 작업 환경에서 npm 레지스트리 패키지 하나가 캐시에 없어 설치 단계까지 재현하지 못했습니다. 대신 package-lock 최상위 의존성 일치(`npm ls --package-lock-only`)와 위 정적/로직 검증을 수행했습니다.
